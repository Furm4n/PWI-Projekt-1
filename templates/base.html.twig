<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <link rel="shortcut icon" type="image/x-icon" href="{{ asset('../images/logo.png') }}"/>
    <title>{% block title %}Hubert Furmański - Programowanie w Internecie{% endblock %}</title>
        <style>/* line 1, ../sass/main.sass */
        .main {
            min-height: 100vh;
            overflow: hidden;
            display: block;
            position: relative;
            padding-bottom: 10vh;
        }

        html {
            scroll-behavior: smooth;
        }

        /* line 8, ../sass/main.sass */
        body {
            height: 100%;
            margin: 0;
            font-family: "Gill Sans", "Gill Sans MT", Calibri, "Trebuchet MS", sans-serif;
            background: radial-gradient(circle, #e5e5e5 0%, #5B5056 100%);
            position: relative;
        }

        /* line 15, ../sass/main.sass */
        header {
            height: 17vh;
            background: radial-gradient(circle, #e5e5e5 0%, #5B5056 100%);
            padding: 5px;
            overflow: hidden;
            position: fixed;
            top: 0;
            width: 100%;
            border-bottom: 3px solid black;
        }

        h1 {
            margin-left: 40px;
            margin-right: 40px;
            padding: 10px;
        }

        /* line 33, ../sass/main.sass */
        footer {
            clear: both;
            text-align: center;
            padding: 5px;
            width: 100%;
            position: absolute;
            bottom: 0;
        }

        /* line 41, ../sass/main.sass */
        a {
            text-decoration: none;
            color: #ffffff;
        }

        /* line 45, ../sass/main.sass */
        .przycisk_menu {
            color: #ffffff;
            text-decoration: none;
            text-align: center;
            font-size: 40px !important;
            margin: 5px;
            cursor: pointer;
            flex-grow: 1;
            background: linear-gradient(rgba(33, 48, 88, 0.4), rgba(33, 48, 88, 0.4));
            padding-top: 6vh;
            padding-bottom: 6vh;
            padding-left: 4vw;
            padding-right: 4vw;
            border: 2px solid #ffffff !important;
            border-radius: 6px;
            display: inline-block;
            margin-left: 4vh;
            margin-top: 32vh;
        }

        /* line 61, ../sass/main.sass */
        .przycisk_menu:hover {
            border-color: black !important;
        }

        /* line 64, ../sass/main.sass */
        .przycisk_menu:active {
            position: relative;
            top: 1px;
        }

        /* line 68, ../sass/main.sass */
        #logo {
            float: left;
        }

        .mainContent {
            background-image: url({{ asset('images/ipad.jpg') }}) !important;
            background-repeat: no-repeat, repeat;
            background-position: center;
            background-size: cover;
            color: white;
            margin-top: 18vh;
            border-bottom: 3px solid black;
            height: 80vh;
            width: 100%;
        }

        #buttonContainer {
            text-align: center;
        }

        p {
            margin: 10px;
        }


        @media (max-width: 1400px) {
            #ph1 {
                height: 10vh;
                width: 100%;
            }

            #logo {
                max-height: 13vh;
                max-width: 13vw;
                float: left;
            }

            header {
                height: 17vh;
                background: radial-gradient(circle, #e5e5e5 0%, #5B5056 100%);
                padding: 5px;
                overflow: hidden;
                position: fixed;
                top: 0;
                width: 100%;
                border-bottom: 3px solid black;
            }

            .przycisk_menu {
                display: list-item;
                list-style: none;
                margin-top: 3vh;
                font-size: 15px !important;
                height: 1px;
                margin-right: 10vw;
                margin-left: 10vw;
            }

            .przycisk {
                float: right;
            }
        {% block stylesheets %}{% endblock %}
        }</style>
</head>
<body>
<div class="main">
    <header>
        <div id="logo">
            <a href="{{ path('main') }}">
                <img src="{{ asset('images/logo.png') }}" style="height: 14vh;" alt="Logo">
            </a>
        </div>
        {% if app.user %}
            <a href="{{ path('app_logout') }}">
                <div class="przycisk">Wyloguj sie</div>
            </a>
        {% else %}
            <a href="{{ path('app_login') }}">
                <div class="przycisk">Zaloguj się</div>
            </a>
        {% endif %}
    </header>
    <div class="mainContent">
        <div id="ph1"></div>
        <div id="buttonContainer">
            <a href="{{ path('projekt') }}">
                <div class="przycisk_menu">Projekt</div>
            </a>
            <a href="{{ path('kontakt') }}">
                <div class="przycisk_menu">Kontakt</div>
            </a>
            <a href="{{ path('o_mnie') }}">
                <div class="przycisk_menu">O mnie</div>
            </a>
            <a href="https://github.com/Furm4n/PWI-Projekt-1">
                <div class="przycisk_menu">Repozytorium</div>
            </a>
        </div>
    </div>
    {% block body %}{% endblock %}
    <footer>
        <p>Hubert Furmański</p>
        <p>Copyright © 2020</p>
    </footer>
</div>
</body>
</html>
